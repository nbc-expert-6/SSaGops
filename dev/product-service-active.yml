apiVersion: v1
kind: Service
metadata:
  name: product-service-active
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: product
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080